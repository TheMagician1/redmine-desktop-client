Index: RedmineManager.cs
===================================================================
--- RedmineManager.cs	(revision 52)
+++ RedmineManager.cs	(working copy)
@@ -171,6 +171,7 @@
                 {
                     using (var xmlReader = new XmlTextReader(text))
                     {
+                        xmlReader.WhitespaceHandling = WhitespaceHandling.None;
                         xmlReader.Read();
                         xmlReader.Read();
                         return xmlReader.ReadElementContentAsCollection<T>();
Index: Types/CustomField.cs
===================================================================
--- Types/CustomField.cs	(revision 52)
+++ Types/CustomField.cs	(working copy)
@@ -42,7 +42,21 @@
             Id = reader.ReadAttributeAsInt("id");
             Name = reader.GetAttribute("name");
             Multiple = reader.ReadAttributeAsBoolean("multiple");
-            Value = reader.ReadElementString();
+            try
+            {
+                Value = reader.ReadElementString();
+            }
+            catch (Exception ex)
+            {
+                try
+                {
+                    Value = reader.ReadElementContentAsString();
+                }
+                catch (Exception ex2)
+                {
+                    Console.WriteLine("Redmine.NET: Could not load custom field value.");
+                }
+            }
         }
 
         public override void WriteXml(XmlWriter writer)
